apiVersion: v2
dependencies:
- name: argo-cd
  repository: https://argoproj.github.io/argo-helm
  version: ^4
description: 'This chart installs and configures:  * ArgoCD itself * A Grafana dashboard
  for ArgoCD * An `OAuthClient` Kubernetes object if the OpenShift 4 option is enabled
  * ArgoCD ApplicationSet * ArgoCD Notifications '
name: ArgoCD
version: "1"
